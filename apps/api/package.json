{
  "name": "@verra/api",
  "version": "1.0.0",
  "private": true,
  "description": "FastAPI backend scripts for Verra",
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "start": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
